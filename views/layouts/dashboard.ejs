<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
</head>

<body class="min-h-screen flex flex-col bg-gray-100">

    <%# navbar-dashboard handles both user (with bell) and admin (no bell) via role check inside it %>
        <%- include('../partials/navbar-dashboard') %>

            <%# Sidebar + page content side by side — only rendered when logged in %>
                <div class="flex flex-1 overflow-hidden">
                    <%- include('../partials/sidebar') %>
                        <main class="flex-1 overflow-y-auto">
                            <%- body %>
                        </main>
                </div>

                <footer class="bg-[#3A0910] text-white/50 text-center py-3 text-xs shrink-0">
                    &copy; <%= new Date().getFullYear() %> University of Cebu — College of Computer Studies. All rights
                        reserved.
                </footer>

</body>

</html>